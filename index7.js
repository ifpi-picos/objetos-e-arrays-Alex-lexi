//Dado o array let numeros = [1,2,3,4,5], use o metodo shift() para remover o primeiro elemento do array.Exiba o array resultante e o elemento removido.

let numeros = [1,2,3,4,5]
let numeroRemovido = numeros.shift(0)
console.log("elemento removido: " , numeroRemovido)
console.log(numeros)